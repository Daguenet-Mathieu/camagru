# msmtprc.template
defaults
auth           on
tls            on
tls_starttls   on
tls_trust_file /etc/ssl/certs/ca-certificates.crt

account        gmail
host           smtp.gmail.com
port           587
from           ${MAIL}
user           ${MAIL_USER}
password       ${MAIL_PASSWORD}

account default : gmail
